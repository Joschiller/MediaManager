<UserControl x:Class="MediaManager.GUI.Components.Pager"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MediaManager.GUI.Components"
             mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="page" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"/>
        <Button x:Name="prev" Grid.Row="1" Grid.Column="0" HorizontalAlignment="Left" Height="24" Width="24" Click="prev_Click" Background="Transparent" BorderBrush="Transparent">
            <Image Source="../../Resources/previous.png"/>
        </Button>
        <Button x:Name="next" Grid.Row="1" Grid.Column="1" HorizontalAlignment="Right" Height="24" Width="24" Click="next_Click" Background="Transparent" BorderBrush="Transparent">
            <Image Source="../../Resources/next.png"/>
        </Button>
    </Grid>
</UserControl>
