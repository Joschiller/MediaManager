<UserControl x:Class="MediaManager.GUI.Controls.List.PartList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MediaManager.GUI.Controls.List"
             mc:Ignorable="d">
    <DockPanel>
        <Button DockPanel.Dock="Top" Click="Button_Click">
            <TextBlock x:Name="title" TextWrapping="WrapWithOverflow"/>
        </Button>
        <Border DockPanel.Dock="Top" Style="{StaticResource defaultBorder}"/>
        <ListView x:Name="list" ItemsSource="{Binding Parts}" SelectionChanged="list_SelectionChanged">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Tag="{Binding Id}" Text="{Binding Title}" TextWrapping="WrapWithOverflow"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </DockPanel>
</UserControl>
