<UserControl x:Class="MediaManager.GUI.Controls.Analyze.AnalyzeModeSelector"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MediaManager.GUI.Controls.Analyze"
             mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Orientation="Vertical" Grid.Column="0">
            <RadioButton x:Name="rbEmpty" Click="rbEmpty_Click"/>
            <RadioButton x:Name="rbDuplicate" Click="rbDuplicate_Click"/>
            <RadioButton x:Name="rbMediaAttribute" Click="rbMediaAttribute_Click"/>
            <RadioButton x:Name="rbPartAttribute" Click="rbPartAttribute_Click"/>
        </StackPanel>
        <DockPanel Grid.Column="1">
            <ComboBox x:Name="attributeSelection" DockPanel.Dock="Top" ItemsSource="{Binding Attributes}" SelectionChanged="attributeSelection_SelectionChanged"/>
            <TextBlock x:Name="description" TextWrapping="WrapWithOverflow" TextAlignment="Justify"/>
        </DockPanel>
    </Grid>
</UserControl>
