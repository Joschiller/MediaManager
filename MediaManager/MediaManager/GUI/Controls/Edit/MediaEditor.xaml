<UserControl x:Class="MediaManager.GUI.Controls.Edit.MediaEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MediaManager.GUI.Controls.Edit" xmlns:atoms="clr-namespace:MediaManager.GUI.Atoms" xmlns:list="clr-namespace:MediaManager.GUI.Controls.List"
             mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <DockPanel Grid.Column="0">
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
                <atoms:ImageButton x:Name="addPart" Size="{StaticResource imageButtonSize}" EnabledIconSource="../../../Resources/add_part.png" Click="addPart_Click"/>
                <atoms:ImageButton x:Name="deletePart" Size="{StaticResource imageButtonSize}" EnabledIconSource="../../../Resources/delete.png" Click="deletePart_Click"/>
            </StackPanel>
            <list:PartList x:Name="list" SelectionChanged="list_SelectionChanged"/>
        </DockPanel>
        <local:ElementEditor x:Name="editor" Grid.Column="1" MediumEdited="editor_MediumEdited" PartEdited="editor_PartEdited"/>
    </Grid>
</UserControl>
